PORT=5000
NODE_ENV=test

# MongoDB (use a test database)
MONGO_URI=mongodb://localhost:27017/ecommerce-test

# JWT
JWT_SECRET=test-jwt-secret

# Email Configuration (disable for tests)
GMAIL_EMAIL=test@example.com
GMAIL_APP_PASSWORD=test-password
USE_EMAIL=false